apiVersion: apps/v1
kind: Deployment
metadata:
  name: ruby-backend
  labels:
    app: ruby-backend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ruby-backend
  template:
    metadata:
      labels:
        app: ruby-backend
      name: ruby-backend
    spec:
      automountServiceAccountToken: false
      containers:
        - image: distributed-trace-test-ruby:latest
          name: ruby-backend
          imagePullPolicy: Never
          ports:
            - containerPort: 8080
          env:
            - name: SERVER_PORT
              value: "8080"
          readinessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 2
            periodSeconds: 5
            timeoutSeconds: 2
      nodeSelector:
        kubernetes.io/os: "linux"
---
apiVersion: v1
kind: Service
metadata:
  name: ruby-backend
  labels:
    app: ruby-backend
spec:
  selector:
    app: ruby-backend
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP
