apiVersion: apps/v1
kind: Deployment
metadata:
  name: rust-backend
  labels:
    app: rust-backend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: rust-backend
  template:
    metadata:
      labels:
        app: rust-backend
      name: rust-backend
    spec:
      automountServiceAccountToken: false
      containers:
        - image: distributed-trace-test-rust:latest
          name: rust-backend
          imagePullPolicy: Never
          ports:
            - containerPort: 8080
          env:
            - name: SERVER_PORT
              value: "8080"
          readinessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 2
            periodSeconds: 5
            timeoutSeconds: 2
      nodeSelector:
        kubernetes.io/os: "linux"
---
apiVersion: v1
kind: Service
metadata:
  name: rust-backend
  labels:
    app: rust-backend
spec:
  selector:
    app: rust-backend
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP
